import 'package:flutter/material.dart';

class body_health extends StatelessWidget {
  const body_health({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MENTAL HEALTH')),
      body: const Center(child: Text('Порожня сторінка')),
    );
  }
}
