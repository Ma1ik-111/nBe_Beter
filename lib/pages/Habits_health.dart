import 'package:flutter/material.dart';

class HabitsHealth extends StatelessWidget {
  const HabitsHealth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MENTAL HEALTH')),
      body: const Center(child: Text('Порожня сторінка')),
    );
  }
}
